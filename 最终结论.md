# YuKi 会话级缓存改动 — 最终审查结论（结论1.md / 结论2.md 逐条验证与裁决）

**审查对象**：`main` @ `9239716` 工作区未提交改动（会话级 TTL 内存缓存：新增 `python-backend/mem_cache.py` + 4 个测试文件，改动 `server.py` / `config.py` / `index.js` / `timeline.js`）。
**验证方法**：两份报告的全部行号与机制描述逐一对照源码复核；所有可实测论断用一次性脚本复现（Python 辅助函数级 + node 表达式对照 + 300 轮并发压力）；两份报告声明的自动化基线全部重跑。

## 〇、基线复核（与两份报告声明一致）

- `python-backend/tests/run_all.py` → **RUN_ALL: ALL PASS**
- `npm run test:jsunit` → **fail 0**（533 全通过）
- `npx eslint src tests scripts` → **0 error / 73 warning**（均为既有项）
- `ruff check python-backend` → **All checks passed**（venv）

**结论：两份报告的自动化验证陈述属实。**

---

## 一、结论1.md 逐条裁决

| # | 论断 | 裁决 | 依据 |
| --- | --- | --- | --- |
| 1 | SSE 整词重放无逃生口 + 部分重放令缺失源卡永久「检索中」 | ✅ **成立**（卡死部分 P1；「重放挡重搜」见备注） | 实测：`put_source` 两源（模拟 error 源不落缓存）→ 索引 `["a","c"]`，重放集合缺 b，b 卡收不到 payload；`_applySourceResult` 初始 `pending`，`done→finish()` 不重置 pending，`_sheetHeaderHtml` 以 `status!=='pending'` 计完成数 → 卡头永久「检索中 N/M」。行号 `server.py:1741`、`kazumi.js:1550`、`search.js:319`（两处均不带 refresh 与 tag/year/sort）全部复核无误 |
| 2 | 单源「重试/重新检索/验证后重查」被缓存挡住 | ✅ **主结论成立**（P1）；❌ **验证码子分支不成立** | 实测 `_cached_kazumi_search`：noresult 单源体被缓存（`_kazumi_search_body_cacheable` 的 `any(not error and not captcha)` 放行无 error 无 captcha 的 noresult 条目），连点两次重试 builder 仅调用 1 次。**但**「命中验证前旧条目」被实测推翻：单源 captcha-only 体整体不可缓存（builder 每次都执行）；单源重查键 `word\|p:X` 与全量检索键 `word` 隔离，验证回调不读全量缓存。验证码场景的真实影响仅是重放时该源仍显示「需验证」，点验证→重查即可恢复，非缺陷 |
| 3 | `_colCacheTs` 回填方向反了 | ✅ **诊断与修复成立**（P2）；⚠️ 影响描述偏大 | node 实测当前公式：仅持久数据年龄 **>25min** 时内存层命中（25.1/26/29min → true；0.1~20min → false），完全倒挂。**其提出的修复（回填 `persisted.ageMs`）正确**。但「紧随其后的 refreshCollections 会立刻重新走网络拉取」不准确：内存判过期后 `_loadColSets` 仍会命中持久兜底（30min TTL 内），**任何公式下网络重拉时机都由持久 TTL 决定**——倒挂的真实影响是内存层职责错位（该复用时判过期、该过期时反复用），不是多付一次网络请求 |
| 4 | `code: 1` 约定被静默拒缓存 | ✅ **机制成立**（证据强度维持「中」） | 实测 `_spider_body_cacheable('detailContent', {"code":1,...}) → False`、无 code → True；`jar_spider._json` 确认原样透传 JVM 输出不裁字段；`cms_spider` 四方法重建 dict（`code` 被剥掉）不受影响。仓库内无 jar 源真实返回 `code:1` 的直接证据 |
| 5 | `mem_cache` 每次写入 O(n) 全表统计 | ✅ **成立** | 实测 100 条 0.004ms → 4000 条 0.096ms（27 倍线性增长，全程持锁）。与结论1自报的 0.38ms 差异来自其测试含实际淘汰，量级与结论一致 |
| 6 | `stats()` 无生产调用方 | ✅ **成立** | 全仓 grep：仅测试引用；`/action do=cacheSize`（`server.py:967`）breakdown 无内存层分项（且渲染层 `refreshCacheSize` 根本不渲染 breakdown 明细） |
| 7 | `destroy()` 不取消在途调用 | ✅ **成立**（窗口窄） | `cms_spider.destroy` 为空实现（`cms_spider.py:128`）；`jar_spider.destroy` 仅 spider 级 `bridge.call('destroy')`，不取消在途 `_call`；`_runtime_site_call` 同步回调 → 热替换清理后旧请求仍可能回写 |
| 观察 | 筛选维度空转 | ✅ **成立** | `kazumi.js:1550` / `search.js:319` 均只传 `word`，fkey 恒为 `'\|\|'` |

> 备注（第 1 条裁决细化）：「重搜同词秒回旧结果」本身正是该缓存的设计目标，不判为功能退化；**真正的退化**是（a）重放时缺失源卡永久「检索中」（P1，必修）、（b）后端对 SSE 完全没有 refresh 逃生口，用户任何显式重搜意图都无法穿透（修复：端点支持 `refresh=1`，UI 暂不增强制刷新入口）。

## 二、结论2.md 逐条裁决

| # | 论断 | 裁决 | 依据 |
| --- | --- | --- | --- |
| 1 | SSE 索引读改写竞态，8 worker 并发丢源 | ⚠️ **缺陷成立，归因描述有误** | 「调用方是 8 个并发 worker」不对：`_kazumi_stream_cache_put_source` 在生成器线程的 `as_completed` 循环里**串行**调用，worker 只跑 `_search_one`。真实竞态窗口是**两个并发的同词同筛选 SSE 流**（如关弹窗后立即重开）。300 轮 8 线程压力（`sys.setswitchinterval(1e-7)` 放大窗口）：3 轮丢登记、累计丢 4 个源——非原子读改写确会静默丢索引项，原子 append 原语的修复方向正确 |
| 2 | 流超时产生被永久重放的部分结果 | ✅ **成立**（TTL 数字勘误） | 代码确认：120s `as_completed` 超时 → `except` 只发 done，超时前完成源的 payload+索引已落缓存，下次整词重放残缺集。**勘误**：stream 缓存在 `kazumi:search` ns（TTL 600s），不是「30 分钟内」而是「10 分钟内」 |
| 3 | `_colCacheTs` 算式令缓存未生效 | ⚠️ **诊断成立**；❌ **其修复公式有缺陷**；❌ 影响不成立 | 算式倒挂成立。但其提议 `min(ageMs, COL_TTL-缓冲)` node 对照验证为**错**：age=26min 时回填 elapsed=4.9min < 5min → 陈旧数据仍被当新鲜命中，倒挂未消除。正确修复为结论1 的直接继承 `ageMs`。「首开省下的网络请求会在第二次进入时又付一次」不成立：第二次进入内存 miss 后仍被持久兜底接住，30min 内网络重拉时机不变（与结论1 #3 勘误同源）。 |
| 4 | `kazumiReorder` 漏失效钩子 | ✅ **成立** | `server.py:2366` 分支复核，全文件仅此变更点无 `_kazumi_invalidate_caches` |
| 5 | `kazumi:search` ns 混装两类数据 | ✅ **成立** | 裸键（全量 body）与 `stream\|` 前缀同 ns；TTL 同为 600s 暂无冲突，但淘汰/失效策略耦合 |
| 6 | `_spider_body_cacheable` 把 `code:None` 当成功 | ✅ **成立**（判定可接受） | 代码确认；扁平 CatVod 响应无 code 属合法成功包络 |
| 7 | `invalidate_prefix`/`stats` 无生产调用方 | ✅ **成立** | 与结论1第 6 条同源；修复后两者均获得生产调用方 |
| 8 | `asset-status` 60s 缓存无失效钩子 | ✅ **成立** | `panels.js:1919-1920` 手动「刷新」按钮与 `refreshAssetStatus()` 全走同一 IPC，无 force 穿透；`index.js:807` handler 无条件命中缓存 |
| 9 | 热替换瞬间 `_cached_spider_content` 在途回写 | ✅ **成立**（与结论1第 7 条同源） | 同上；窗口窄，注释补强即可，代际键方案过度设计 |

## 三、合并修复清单（本次实施）

**P1**
1. `server.py` SSE：新增 `refresh` 参数（=1 跳重放）；重放按当前 `searchable_plugins` 对缺失源补发 `error` 终态占位 payload（带「重试」按钮语义，修卡死）；流超时/异常/客户端断连（GeneratorExit 绕过常规 if 检查，属修复过程中新发现的第三类中断路径）时对 `word|fkey` 的索引与 payload 整词失效，禁止残缺结果参与重放。
2. `kazumi.js` `_queryPlugin`：重试/重新检索/手动检索/验证后重查统一带 `refresh=1`（显式用户动作穿透读缓存，后端回写语义不变）。
3. `mem_cache.py`：新增 `mutate()`（锁内原子读-改-写）与 `delete_value()`；`server.py` 索引登记改用 `mutate`，消除读改写竞态。
4. SSE 流缓存拆独立 ns `kazumi:stream`（结论2 #5），失效钩子同步扩两 ns。

**P2**
5. `timeline.js:230`：回填改为 `Date.now() - persisted.ageMs`（内存 age 直接继承持久 age；采用结论1方案，弃用结论2的 `min` 公式），并修正被宽松断言掩盖的测试用例。
6. `mem_cache.py`：条数/字节改增量计数器维护，稳态写入 O(1)，仅超限时扫描淘汰（结论1 #5）。
7. `_spider_body_cacheable` 判定放宽：`code` 白名单 → 失败黑名单（`error` 字段 / code≤0 / code≥400 拒缓存；`code:1` TVBox 旧约定放行）（结论1 #4）。

**P3**
8. `kazumiReorder` 补 `_kazumi_invalidate_caches()`（顺序影响重放登记序与卡片推送序）。
9. `stats()` 返回条目数+字节并接入 `cacheSize` breakdown（`memCache`/`memCacheItems`）；`invalidate_prefix` 修复后已有生产调用方（整词失效），注释同步更正。
10. `asset-status`：IPC handler 与 `preload` 增 `force` 参数；`panels.js` 手动刷新、mpv 下载完成、播放器 spawn 失败三处走 force 重探。
11. `config.py` 失效注释补强：明示在途 `builder` 回写窗口未被覆盖、属接受代价（结论1 #7 / 结论2 #6）。

**不做**（有意取舍）：前端不加「整词强制重搜」按钮（同词秒回即设计目标，refresh 逃生口已开后端能力）；不在 SSE 传 tag/year/sort（维持现状，fkey 空转属预留设计）；不做按站点取消在途调用/配置代际键。

---

## 四、修复实施与回归结果

### 修复实施（对应第三节编号）

| # | 文件 | 实施内容 |
| --- | --- | --- |
| 1 | `python-backend/server.py` | `/search/kazumi-stream` 增 `refresh` 参数（`_form_flag` 口径）；重放前取 `searchable_plugins`，对缺失源补发 `{error:true, status:'error'}` 终态 payload（卡面出「重试」按钮，修复永久「检索中」）；`as_completed` 循环以 `complete` 标记裁决，超时/异常/**客户端断连（GeneratorExit）**三条中断路径统一在 `finally` 中整词失效（新增 `_kazumi_stream_cache_invalidate_word`） |
| 2 | `src/renderer/js/kazumi.js` | `_queryPlugin` 固定带 `refresh: '1'`——重试/重新检索/手动检索/验证后重查四类显式动作全部穿透读缓存（后端回写语义不变） |
| 3 | `python-backend/mem_cache.py` | 新增 `mutate()`（锁内原子读-改-写，禁止 mutator 重入）与 `delete_value()`；`server._kazumi_stream_cache_put_source` 的索引登记改走 `mutate`，消除跨流读改写竞态 |
| 4 | 同上 + `server.py` | SSE 流缓存（单源 payload + 整词索引）拆独立 ns `kazumi:stream`（DEFAULT_TTL 600s）；`_kazumi_invalidate_caches` 同步清 search+stream 两 ns |
| 5 | `src/renderer/js/timeline.js` | `_colCacheTs = Date.now() - persisted.ageMs`（采用结论1 方案，弃用结论2 `min` 公式），注释写明网络重拉时机由持久 TTL 决定 |
| 6 | `python-backend/mem_cache.py` | 条数/字节改模块级增量总账（`_put_locked/_drop_locked` 单点维护），`_evict_locked` 稳态判断 O(1)、仅超限时线性扫描；`get/mutate/invalidate/invalidate_prefix/clear_all` 全部同步总账 |
| 7 | `python-backend/server.py` | `_spider_body_cacheable` code 判定：白名单 `(200, None)` → 失败黑名单（`error` 字段 / code≤0 / code≥400 / 非数值 code 拒绝；`code:1` TVBox 旧约定与数字字符串同值化后放行） |
| 8 | `python-backend/server.py` | `kazumiReorder` 成功后补 `_kazumi_invalidate_caches()` |
| 9 | `python-backend/mem_cache.py` + `server.py` | `stats()` 升级返回 `{ns: {items, chars}}` 并接入 `/action cacheSize` breakdown（`memCacheItems`/`memCacheChars`，不计磁盘总量）；`invalidate_prefix` 注释更新——修复后已有生产调用方（整词流失效） |
| 10 | `src/main/index.js` + `src/preload/preload.js` + `src/renderer/js/panels.js` | `yuki:asset-status` 增 `force`：跳过 60s 整体缓存并穿透 Java 探测缓存；渲染层「手动刷新」、mpv 下载完成、播放器 spawn 失败三处走 force（修「装完 Java/下载完播放器后 60s 内状态不变」） |
| 11 | `python-backend/config.py` | 热替换注释补强：明示 `destroy()` 不取消在途调用、回写窗口接受不闭环的取舍依据 |

### 测试变更（回归锁）

- `test_mem_cache.py`：+`test_mutate_atomic_append`、`test_mutate_edge_cases`（None 不写/超长拒绝/过期视同缺失/非法参数）、`test_delete_value`（幂等+总账归零）、`test_global_accounting_consistency`（淘汰/覆盖写/前缀失效/整 ns 后总账与 `_store` 实账一致）；`test_stats` 适配新结构。
- `test_kazumi_cache.py`：stream 键改断言 `kazumi:stream` ns；+`test_stream_cache_invalidate_word`（整词失效不误伤他词/他筛选）、`test_stream_missing_names`（补齐判定+坏 payload 不视为已覆盖）；`test_invalidate_helpers` 覆盖三 ns 范围。
- `test_spider_content_cache.py`：+`code:1`/`code:"1"` 可缓存、`code:0/-1/500/"abc"` 拒缓存回归断言。
- `tests/js/timeline-cache.test.js`：原宽松断言（`<= 30min` 持久 TTL 放过倒挂）收紧为「新快照恢复后内存 age≈0 + 立即二次命中零网络」；+陈旧快照（20min）用例锁定「内存 age 继承持久 age、持久 TTL 内零网络」。

### 回归结果（最终代码实测）

- `python-backend/tests/run_all.py` → **RUN_ALL: ALL PASS**
- `npm run test:jsunit` → **534 pass / 0 fail**（+1 新用例）
- `ruff check python-backend` → **All checks passed**
- `eslint`（本次改动的 6 个 JS 文件）→ **0 error**（22 warning 均为既有未使用项）
- 竞态复压：修复后 `put_source` 经 `mutate` 原子化，索引读改写竞态窗口消除（payload 写与索引写在同一临界点收敛）

### 遗留（有意不做）

- 前端无「整词强制重搜」按钮（同词秒回为设计目标；后端 refresh 逃生口已具备，未来加 UI 一行即通）。
- SSE `tag/year/sort` 渲染层仍不传（fkey 空转属预留设计，非缺陷）。
- 配置热替换在途回写窗口不闭环（注释已说明取舍）；`jar` 源 `code:1` 真实样本仍待线上验证，但判定已放宽，即便不验证也不再拒缓存。
