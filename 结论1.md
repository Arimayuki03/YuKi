# YuKi 会话级缓存改动代码审查报告

**审查范围**：会话内 TTL 内存缓存引入（新增 `python-backend/mem_cache.py`，改动 `server.py` / `config.py` / `src/main/index.js` / `src/renderer/js/timeline.js`），共 5 个文件改动 + 4 个新增测试文件，约 480 行新增。
**仓库状态**：`main` 工作区未提交改动（基于 `9239716`）。
**自动化验证**（本次实测）：

- `python-backend/tests/run_all.py` → **RUN_ALL: ALL PASS**（含新增 3 个 stage）
- `npm run test:jsunit` → **533/533 通过**
- `ruff check python-backend` → **通过**（0 问题）
- `npx eslint src tests scripts` → **0 error / 73 warning**（均为既有未使用变量告警，非本次引入）

## 总体评价

这是一次设计意图清晰、失效点覆盖得比同类实现更细的缓存改动。值得肯定的是：四层缓存（spider 内容 / kazumi 搜索 / kazumi 章节 / SSE 单源）各自独立命名空间，写缓存全程 try 包裹保证缓存故障不影响主链路，"非空才缓存"守卫避免了把故障冻结成缓存，配置热替换与插件增删的失效钩子也基本补齐了。

但有 **2 处会造成用户可见功能退化的问题**，且都在"显式用户动作被缓存挡住"这一类上——缓存层没有为 UI 的重试语义留出口。另有 2 处逻辑/判定问题、3 处工程化问题。

---

## 一、严重：用户可见的功能退化

### 1. SSE 整词重放吞掉「换词重搜」，且部分重放会让源卡永久停在「检索中」

`python-backend/server.py:1741`

```python
cached_payloads = _kazumi_stream_cached_payloads(word, fkey)
if cached_payloads:
    for payload in cached_payloads:
        yield f'data: {payload}\n\n'
    yield 'event: done\ndata: {}\n\n'
    return
```

重放分支不区分"这是不是一次全新的搜索"，只要 `word` + 筛选指纹相同就直接重放并 `return`。而渲染层两处 SSE 发起都**不带任何 refresh 参数**：

- `src/renderer/js/kazumi.js:1550`（选源弹窗）
- `src/renderer/js/search.js:319`（搜索页 Kazumi 页签）

所以没有任何路径能穿透这层缓存。用户关掉选源弹窗再打开同一部片、或想重新搜一遍，拿到的永远是 10 分钟内的第一次结果。

**更严重的是部分重放**。`_kazumi_stream_cache_put_source`（`server.py:2161`）按设计不缓存 error 结果，索引里因此只登记成功源。若上次检索中某个源报错，索引就只有其余源。我实测确认：

```
重放集合 = ['{"source": "kazumi:a", ... "status": "success"}']
replay 非空 → 走重放分支并 return: True
```

渲染层的源卡状态在 `kazumi.js:1543` 初始化为 `pending`，只有收到该源的 payload 才会被 `_applySourceResult` 翻转。重放时缺失的源永远收不到推送，`done` 事件触发 `finish()`（`kazumi.js:1558`）只关闭流、更新头部，**不重置 pending 卡**。用户看到的是那张卡永久停在「检索中」，头部进度也永远显示「检索中 N/M」。

**修复方向**：重放需要与"用户主动重搜"区分。可选：SSE 端点接受 `refresh=1` 时跳过重放；或重放时对索引中缺失的源补发一条 `error`/`noresult` 占位 payload，让前端把卡片翻到终态。

### 2. 单源「重试 / 重新检索 / 验证后重查」全部被缓存挡住

`python-backend/server.py:2127` `_cached_kazumi_search`

`_queryPlugin`（`kazumi.js:1599`）是三类用户动作的统一入口：卡片上的「重试」「重新检索」按钮（`kazumi.js:1682`）与验证码通过后的自动回调（`kazumi.js:1781`）。表单固定为 `{keyword, plugin}`，**同样不带 refresh**。

实测（`plugin` 过滤路径，键为 `海贼王|p:enlie`）：

```
retry #1 -> {"code": 200, "results": [{"pluginName": "enlie", "data": [{...old}]}]}
retry #2 -> {"code": 200, "results": [{"pluginName": "enlie", "data": [{...old}]}]}
builder calls = [1]        ← 点两次「重试」，只查了一次源
```

问题在于 `_kazumi_search_body_cacheable`（`server.py:2110`）的守卫只排除"空结果 / 全 error / 全 captcha"。单源 `status: "noresult"` 的条目满足"存在非 error 项"因此**照常缓存**，实测确认：

```
A) noresult 是否被缓存: True | builder 调用次数: 1
   点「重新检索」(同表单) 返回: noresult (仍是旧结果，未重查)
```

两个具体后果：

- 用户看到「无结果」→ 点「重新检索」→ 拿回同一条 noresult，直到 10 分钟 TTL 过期。UI 上按钮点了等于没点。
- 验证码场景更别扭：源因 captcha 失败时会写入一条 captcha 结果（`captcha` 为真但仍是"非 error"项，只要同批有其它成功源就整体可缓存），用户过完验证码回调重查，命中的是验证前的旧条目，表现为"验证了但源没恢复"。

**修复方向**：`plugin` 过滤的单源重查属于显式用户动作，应带 `refresh=1`，或后端对带 `plugin` 的请求不读缓存（仍回写）。

---

## 二、中等：逻辑与判定

### 3. 时间表持久缓存的 TTL 回填方向反了

`src/renderer/js/timeline.js:230`

```js
this._colCacheTs = Date.now() - Math.max(0, TIMELINE_COL_PERSIST_TTL - persisted.ageMs);
```

想表达"持久层剩余寿命映射到内存 TTL"，但减号方向反了：实际回填的是 `30min - ageMs`，而语义上应当是 `ageMs`（已消耗的年龄）。实测（内存 TTL 5min，持久 TTL 30min）：

| 持久数据已过去 | 命中 ageMs | 内存缓存被回填成的年龄 | 应 |
| --- | --- | --- | --- |
| 0.1min | 0.1min | **29.9min** | 0.1min |
| 5min | 5.0min | 25.0min | 5.0min |
| 20min | 20.0min | 10.0min | 20.0min |
| 26min | 26.0min | 4.0min | 26.0min |
| 29min | 29.0min | 1.0min | 29.0min |

语义完全倒挂：**越新鲜的持久数据，在内存层被判得越旧**（ageMs=0.1min 时内存年龄 29.9min > 5min TTL，立即判过期）；越陈旧的反而被当作新鲜复用。

影响是"加速效果打折"而非数据陈旧——本次上屏仍由持久数据完成（白屏等待确实被消除，这是该改动的主要目的，达成），只是紧随其后的 `refreshCollections` 会立刻重新走网络拉取，内存层这一跳没兜住。修正是把 `TIMELINE_COL_PERSIST_TTL - persisted.ageMs` 换成 `persisted.ageMs`。

### 4. `code: 1` 成功约定的源会被静默拒绝缓存（证据强度：中）

`python-backend/server.py:2286`

```python
if not isinstance(d, dict) or d.get('code') not in (200, None) or d.get('error'):
    return False
```

仓库内 `python-backend/tests/fixtures/q7_offline_fixtures.py:54` 的 `SAMPLE_JSON_CMS` 明确以 `"code": 1, "msg": "数据成功"` 表示成功，这是 TVBox/FongMi 生态的真实约定。

分两条链路看，结论不同：

- **内置 CMS 源**（`cms_spider.py`）不受影响——它重建 dict（`homeContent`/`categoryContent` 等只取 `class`/`list`/`page` 字段），`code` 被剥掉，判定命中 `None` 分支。这条链路的缓存是生效的。
- **jar 源**风险在于 `jar_spider.py` 的 `_json`（第 513 行）对 JVM 返回**原样透传**，不做字段裁剪。若某个 jar spider 的 `homeContent` 返回带 `code: 1` 的结构，四个读接口会全部静默不缓存。

我在仓库内没有找到 jar 源返回 `code:1` 的直接证据，因此标为"中"而非"确定"。建议实测几个真实 jar 源确认，或把判定放宽为「明确失败码黑名单 + 空结果判据」，而不是「成功码白名单」。

---

## 三、工程化

### 5. `mem_cache` 每次写入都做一遍 O(n) 全表统计

`python-backend/mem_cache.py:70-86` 的淘汰循环每轮都重新遍历**所有命名空间**求 `total` 与 `chars`（两个 `sum`），再线性扫一遍找最久未访问条目，且全程持 `_lock`。

实测（单条 value 200 字符）：

| 场景 | 单次写入耗时 |
| --- | --- |
| 100 条规模 | 0.004 ms |
| 4096 条满载 | **0.38 ms**（约 95 倍） |

成本随条目数线性增长，且发生在请求线程内并持锁。当前 64MB / 4096 条预算下不构成问题，但建议把条数与字符数维护为增量计数器，仅在真正超限时才扫描淘汰，把稳态成本降到 O(1)。

（更正：我上一轮口头描述为"O(n²)"，实测表明是**每次 set 一次 O(n) 扫描**，非 O(n²)。）

### 6. `mem_cache.stats()` 没有生产调用方

`mem_cache.py:134` 的 `stats()` 注释写"设置面板缓存统计用"，但全仓 grep 只有测试引用它。`/action do=cacheSize`（`server.py:967`）与设置面板的缓存展示都未把内存层计入，`clearCache` 的 `detail` 里也没有内存层条目数。要么接进 `cacheSize` 分项，要么删掉，否则会误导后续维护者以为统计已闭环。

### 7. 在途回填窗口：`destroy()` 不取消在途调用

`config.py:1120-1126` 把 `invalidate` 放在旧 runner 销毁之后，注释里的论证（避免在途回填让旧内容以新站点 key 存活）方向是对的。但 `_runtime_site_call`（`server.py:595`）走的是同步 callback，而 `cms_spider.destroy()` 是空实现（`cms_spider.py:128`）、`jar_spider.destroy()` 也不取消在途调用。因此"销毁后旧请求仍回写"这条路径概率虽小但客观存在。

若要彻底闭环，可在 `_apply` 中按站点取消在途调用，或让缓存键带上配置代际（`swap_count`）。

### 观察：筛选维度当前是空转的

`_kazumi_stream_filters_key` 设计上让 `tag/year/sort` 参与缓存键，文档也强调"不同筛选互不串台"。但渲染层两处 SSE 发起（`kazumi.js:1550`、`search.js:319`）都不传这三个参数，`fkey` 恒为 `'||'`。缓存键的这一维度目前不起作用——不是缺陷（预留设计），但值得知道它尚未被真实调用。

---

## 四、做对的地方（明确排除）

- **失效点覆盖**：插件增删（`kazumiAdd`/`kazumiRemove`/`kazumiShopInstall`）、开关（`kazumiToggle`）、有效性检测（`kazumiCheckValidity`，只清搜索 ns）、批量更新（两者都清）、Cookie 变更/清空、镜像切换、设置面板全清——逐一核对，该清的都清了，且"只清 search 不清 chapters"的取舍（章节以 `(plugin_name, src)` 为键，与插件集合无关）是有依据的。
- **非空守卫**：kazumi 搜索排除了空结果/全 error/全 captcha；章节排除空 `roads`；spider 排除空 `list`/`class`。口径一致且都有注释说明防的是"瞬时故障被钉死整个 TTL"。
- **缓存层不影响主链路**：`_cached_kazumi_search` / `_cached_spider_content` / SSE 写缓存全部 try 包裹，`builder` 保证至多执行一次（无重复查源）。
- **命名空间隔离**：`config.py` 热替换只清 `spider:*` 四个 ns，不用 `clear_all`，避免连带清掉与站点配置无关的 kazumi ns——这个细节做得对。
- **`_spider_cache_key` 的参数规范化**：只纳入真正影响结果的字段（忽略 token/refresh），顺序固定；`searchContent` 的 `word` 正确复用了 `key` 别名回退（`server.py:2270`），避免只传 `key` 的请求落到空 word 键造成跨关键词串结果。
- **测试质量**：4 个新增测试文件覆盖了 TTL、LRU、ns 隔离、超限拒绝、键规范化、命中/回写/refresh 语义、错误不缓存、部分重放跳过、空重放视作 miss——边界挑得准。`run_all.py` 登记齐全，我已逐个单独运行确认 PASS。`tests/js/timeline-cache.test.js` 由 `run-jsunit.js` 自动发现，无需额外登记。
- **`asset-status` 缓存**（`index.js:250-275`）：成功长期缓存 / 失败只缓存 60s 的差异化策略是对的（装了 Java 后不必重启），同步探测无在途并发所以不需要 pending 去重——注释里的这个论证成立。注意 `mpv` 路径下 `if (!mpv.binary) mpv.resetBinary()` 仍在缓存未命中时才执行，而设置页「下载内置播放器」成功后会调 `refreshAssetStatus()`，若距上次查询不足 60s 会命中旧结果返回 `mpv.ready: false`，按钮因此不隐藏（`panels.js:2372`）。属轻微体验瑕疵，非功能故障。

---

## 建议修复顺序

1. `server.py:1741` —— 重放分支补 `refresh` 逃生口，并对索引缺失源补发终态 payload（修「检索中」卡死 + 换词重搜）
2. `kazumi.js:1599` / `server.py:2127` —— 单源重查带 `refresh=1`，或后端对 `plugin` 请求不读缓存（修「重试无效」）
3. `timeline.js:230` —— 回填改为 `persisted.ageMs`（一行）
4. `server.py:2286` —— 实测 jar 源 `code` 字段后放宽判定
5. `mem_cache.py:70` —— 淘汰改增量计数；`stats()` 接进 `cacheSize` 或删除

第 1、2 项是用户可感知且有实测证据的功能退化，建议在合入前处理。
