# YuKi 项目审查报告

> 审查日期：2026-09-17 · 审查范围：Electron 主进程、Python 后端、渲染层、构建/CI/测试基础设施
> 说明：所有 P1 发现均已人工复核代码证据确认属实。

**总体评价：架构与安全基线在同类项目中属于上乘，但存在 8 个必然触发或可被利用的 P1 级缺陷。** 无 P0（渲染进程可直接任意读写/执行）漏洞：`contextIsolation:true` + preload 白名单、爬虫沙箱限额、Cookie 加密落盘、日志脱敏、二进制 sha256 锁定、XSS 转义纪律都做得认真。问题集中在**生命周期清理、并发竞态和少数漏网的透传字段**上。

---

## P1 —— 必修项

### Electron 主进程

**1. 下载去重的「已完成」判定完全失效。**
`src/main/dl-dedupe.js:17` 的 `DEFAULT_FS = { existsSync: () => false }` 是一个永远返回 false 的桩，而生产代码（`src/main/index.js:281`）从未调用 `setFs()` 换成真实 fs —— 只有测试（`tests/js/dl-dedupe.test.js:20`）调了。结果 `check()`（dl-dedupe.js:98）的 complete 分支永远找不到文件、永远放行重下：开启边下边播后，重播任何已下载剧集都会重复建任务重复下载；`yuki:dl add` 的 `already-done` 分支永不命中。注释「生产环境无需调用」与实际行为矛盾。
**修复**：模块内默认用真实 `fs`，仅测试注入替身（一行）。

**2. python-bridge 的 stdout 缓冲无限增长（内存泄漏）。**
`src/main/python-bridge.js:102-119` 中 `buf += text` 后匹配到 READY 也不截断、不重置，而 Python 后端所有日志都走 stdout。长时间运行内存随日志量线性增长，且每次都对全量 buf 重跑正则，CPU 开销同步上升。
**修复**：匹配成功后置空 `buf`，并限制长度（如 `buf = buf.slice(-1024)`）。

**3. 后端崩溃重启定时器与 stop/start 竞态 → 双后端进程。**
`src/main/python-bridge.js:136` 的 `setTimeout(() => this._spawn(), delay)` 句柄未保存，`stop()`（:190）无法取消已在途的重启定时器。后端崩溃后 1 秒内若用户触发 `bridge.stop(); bridge.start()`（`yuki:set-proxy` / `yuki:set-dandan` / `yuki:pick-cache-dir` 等），会 spawn 出两个后端并存：内存翻倍、Worker 池翻倍、旧进程变孤儿占着端口。
**修复**：保存 timer 句柄，`stop()`/`start()` 时先 `clearTimeout`。

**4. 退出清理调用了不存在的 `hls.cleanup()`，HLS/ffmpeg 子进程退出时不被杀。**
`src/main/index.js:4056` 的 `if (hls && hls.cleanup)` 永远为 false 静默跳过 —— `hls-downloader.js` 根本没有 `cleanup` 方法。aria2/mpv/python 退出都有完整击杀链，唯独 HLS 侧的 ffmpeg 子进程和分片临时目录（`.incomplete`/segs）退出时不收尾。
**修复**：为 HlsDownloader 实现 `cleanup()`（遍历任务杀进程、清定时器、删临时目录），并在 `runQuitCleanup` 中调用。

### Python 后端

**5. `JarBridge.destroy_all()` 死锁（退出路径必然挂死）。**
`python-backend/jar_bridge.py:405-416` 持有 `_jar_bridges_lock`（`threading.Lock`，不可重入，:263）循环调用 `b.destroy()`，而 `destroy()` 内部（:1221）再次获取同一把锁 → 同线程二次获取永久死锁。只要 JVM 桥存在，应用退出 shutdown（`server.py:1537`）即挂死，JVM 进程也无法回收。
**修复**：锁内摘除全部条目并 `clear()`，锁外再逐个 destroy（同文件 `_evict_jvm_if_needed_locked` 已示范该正确模式）。

**6. `go_proxy._put` 捕获了错误的异常类型 → 播放流永久挂死。**
`python-backend/go_proxy.py:1153-1162` 对 `q.put(item, timeout=1.0)` 捕获 `queue.Empty`，但队列满时抛的是 **`queue.Full`**。消费端背压超过 1 秒（播放卡顿、磁盘慢）时异常炸掉下载线程；`stream()` 的 `q.get()`（:1212）无超时无取消检查，mpv 侧流挂死、下载线程泄漏，只能重启播放。触发条件：>32MB Range 的分段下载（go_proxy.py:1788）。
**修复**：改为 `except queue.Full`；同时给 `q.get()` 加超时并检查 `self._cancel`。

**7. `/proxy` 数据面免 token + 任意 URL 转发 = 本机开放代理 / SSRF。**
`server.py:86` 将 `/proxy` 列入 TOKEN_EXEMPT，`hoststate.py:101-105` 空 token 直接放行；`go_proxy.py:1333-1430` 的 `?url=` 通用转发只检查 `http(s)://` 前缀，无私网限制。浏览器有 `_reject_browser` 兜底、Cookie 附加有域名白名单，但**本机任何非浏览器进程**无需凭据即可把后端当开放代理：探测内网、拉取任意资源、经 `?url=` 指向网盘域名白嫖 Cookie 通道。
**修复**：对 `?url=` 通道要求 `valid_proxy_token`（jar 生成播放 URL 时已注入 token，见 jar_bridge.py:557-566，无兼容障碍）；或加私网地址黑名单开关。

### 渲染层

**8. 第三方源错误文本未转义直入 innerHTML → XSS。**
`src/renderer/js/home.js:1630` 与 `src/renderer/js/detail.js:385-386` 把后端透传的 `data.error`（jar 蜘蛛/源站失败原因，**用户配置的第三方源可控**）经 `errorTextOf`（common.js:432，只截断不转义）拼进 `.html()`。渲染层无 CSP，恶意源返回的错误串（100 字符内即可放置 `<img src=x onerror=...>`）可在渲染进程执行 JS、调用 `window.yuki` IPC 桥。项目的 escHtml 体系很完整，这是少数漏网点。
**修复**：对 `err` 包一层 `escHtml`（成本极低）。

---

## P2 —— 重点建议（按性价比排序）

### 安全加固

| # | 位置 | 问题 |
|---|------|------|
| 1 | `src/main/index.js:539-620` | 主窗口缺 `will-navigate` 拦截（parse-window/pan-qr-window 都有，主窗反而没有）；渲染层无 CSP meta —— XSS 缺第二道闸 |
| 2 | `src/main/index.js:1539` | `yuki:push-url` 无协议白名单，`file:///*.m3u8` 可直达 mpv，绕过 `yuki:play` 的 protoOk 校验 |
| 3 | `scripts/binaries.lock.json:13-16`、`src/main/ffmpeg.js` | ffmpeg 下载 sha256 为 null（90MB 未 pin 的可执行文件，全链路唯一缺口）；`https.get` 无 timeout —— 网络挂起时所有 m3u8 下载永远报「ffmpeg 下载中」直到重启 |
| 4 | `python-backend/server.py:2381` | `kazumiImageSearch` 无响应体大小限制（同文件 `/kazumi/cover` 有 8MB 门），内存耗尽 + 不经 guard_url 的 SSRF 出口 |
| 5 | `python-backend/server.py:1562` | 主 token 比较用 `!=` 而非 `hmac.compare_digest`（同文件 valid_proxy_token 已是正确写法） |
| 6 | `python-backend/jar_bridge.py:557` | 数据面 token 经 JVM 命令行传播，同用户进程可枚举；与控制面 token 同值 |
| 7 | `src/main/push-server.js:39,56,68` | 绑 0.0.0.0（LAN 推送设计使然，有 token 门控），但 body 超 64KB 悬挂不响应；畸形请求行抛进全局 uncaughtException 而非回 400；token 走 GET query 留存浏览器历史 |
| 8 | `src/main/index.js:204-248` | Anime4K 镜像含第三方加速代理（ghfast.top），完整性仅查子串；GLSL 会被 mpv 执行，被篡改着色器可过校验；写入机器级安装目录（Program Files）无写权限时静默失败 |
| 9 | `src/main/index.js:3707` | `taskkill /PID` 杀上一个外部播放器有 Windows PID 复用风险，建议先校验进程映像名 |

### 正确性 / 性能

| # | 位置 | 问题 |
|---|------|------|
| 10 | `src/main/system-proxy.js:95` | 主线程 `execSync` 查注册表（TTL 5s），下载活跃期每 5 秒阻塞主进程 ~50ms |
| 11 | `python-backend/pan_login.py:186` | 死条件：遍历的是 cookie 名却判断 `'quark.cn' in name`，恒 False；兜底收集不滤域，碰巧可用 |
| 12 | `python-backend/server.py:1281` | 聚合搜索每请求新建 16 线程池；`kazumi_search_stream` 超时后 `shutdown(wait=False)` 泄漏非 daemon worker |
| 13 | `python-backend/jar_bridge.py:984` | 热重载 `destroy()` 与在途 `call()` 竞态：写 stdin 不持锁，可复活已销毁 JVM（进程泄漏） |
| 14 | `src/renderer/js/kazumi.js:1185` | `_pollTask` 对空响应无限轮询无上限（对比 `app.js:209` 有 100 次上限） |
| 15 | `src/main/index.js:2506` | `yuki:dl add` 文件名兜底缺 `.`/`..` 防御（aria2 路径无 basename 检查） |
| 16 | `src/main/index.js:1656` | `yuki:captcha-verify` 超时返回 `ok:true`，成功与超时语义混淆 |
| 17 | `python-backend/jar_bridge.py:338-361` | `_jar_download_locks` / `_jar_integrity_warned` 按 URL setdefault 永不清理（无界增长，量级小） |
| 18 | `src/main/playlist-proxy.js:435` | Range 头 start > EOF 时算出负 Content-Length，靠外层 catch 回 502，应显式回 416 |
| 19 | `src/main/mpv-player.js:131-152` | 构造器内 `supportsContextMenu` 探测结果被无条件覆盖，字段成死状态 |

### 工程卫生

| # | 位置 | 问题 |
|---|------|------|
| 20 | `package.json` / `package-lock.json` / `README.md:20` | 版本三处漂移：0.2.4 / 0.2.2 / 0.2.2（lock 未随 bump 刷新，下次 `npm ci` 出 diff；README 落后两个小版本） |
| 21 | 仓库根目录 | `pnpm-lock.yaml`（一个月未更新）与 `package-lock.json` 并存，CI 全走 npm，应删；根目录遗留 `paint-order-test.png`、空的 `vlc-help.txt` 已入库，建议 `git rm` |
| 22 | `.github/workflows/ci.yml` | CI 的 js job 不跑 ESLint（本地 `test:all` 含 lint 但 CI 缺失）；release.yml 不等 CI 通过即可发 Draft Release |
| 23 | `tests/js/home-probe.test.js:26` | escHtml 零专属测试，且测试桩还是修复前的旧四规则版本（缺 `'` 转义），与真实实现漂移 |
| 24 | `src/renderer/js/detail.js:937,1375` | 吐槽回复模板两份近似拷贝；日期格式化三套实现，应抽公共函数 |
| 25 | `python-backend/server.py:2256` | kazumiBangumiStaff 分支 return 后有 3 行复制粘贴残留死代码；`proxy_gateway.py:24-27` 亦有恒不可达分支 |
| 26 | `python-backend/server.py:864-1237,1989-2467` | `_dispatch_action_inner`（~370 行）与 `dispatch_kazumi_action`（~480 行）超长；go_proxy 中 quark 链路两份实现已有漂移迹象 |
| 27 | `src/renderer/` | jQuery 3.7.1 与原生 API 混用的一致性债（已知技术债，文件头有登记） |

---

## 确认无问题的重点项

| 关注点 | 结论 |
|---|---|
| 渲染进程隔离 | 主窗口/解析窗口 `contextIsolation:true, nodeIntegration:false`，隐藏/远程页面 sandbox:true；preload 仅暴露白名单通道 |
| zip slip | 无 extractall；jar 逐条 read/writestr 不落盘解压 |
| 爬虫执行沙箱 | Python 走子进程、JS 走 QuickJS（CPU 30s/内存 256MB 限额）、JAR 独立 JVM；崩溃重启有 3 次上限 |
| 配置层 SSRF | fetch_guarded 每跳过 guard_url，DNS 解析判私网，有解压炸弹/长度/跳数限制 |
| Cookie 存储 | DPAPI/AES-GCM 加密落盘 + 原子写；日志明确不输出内容 |
| 日志脱敏 | _RedactingFormatter 覆盖 token/cookie/authorization/password |
| 监听地址 | uvicorn 与 go_proxy 全部绑 127.0.0.1 |
| XSS 转义纪律 | 抽查主要 .html() 拼接点，不可信字段基本都包 escHtml；BBCode 先转义再白名单替换顺序正确 |
| 二进制供应链 | mpv/aria2/anime4k/misans 全部 lock + sha256 双向校验，已存在文件也复验 |
| 事件监听卫生 | off/on 配对、单例守卫、dlgToken 作废在途异步 |
| 仓库卫生 | .test-runtime 等运行态目录均被 .gitignore 正确覆盖，无敏感文件入库 |

## 值得肯定的设计

- mpv 会话所有权保护（代际/活跃会话判断）避免旧请求误杀新会话；teardown 同步 reject 全部 pending IPC。
- 外部播放器启动的 single-flight + 串行链对竞态双窗的分析和修复完整。
- playlist-proxy 的清单收取总死线（防「慢滴不结束」）、上游断流终止响应等是对真实播放器 bug 的有效加固。
- 下载引擎崩溃自愈（exit/error 双路径）与退出清理幂等锁设计合理。
- Python 后端依赖校准 + 导入守卫直接针对真实事故修复，工程判断好。

## 建议修复顺序

**1 → 5 → 6 → 2 → 8**（都是小改动高收益：一行修 fs 桩、锁外销毁、改异常类型、截断 buf、包 escHtml）
→ **3 → 4 → 7**（重启定时器句柄、补 hls.cleanup、数据面鉴权）
→ P2 按需：先做安全类 1/2/3（will-navigate + CSP、push-url 白名单、ffmpeg pin+超时），再做正确性类 10/11/13，最后工程卫生。
