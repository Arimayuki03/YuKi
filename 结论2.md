# 代码审查报告：未提交改动（会话级缓存提速）

**审查范围**：工作区未提交改动 —— 5 个修改文件 + 5 个新增文件（约 480 行修改 + 944 行新增）。
**仓库状态**：`main` @ `9239716`，版本 0.2.4。
**自动化验证**（本次实测）：Python 新增用例 30/30 ALL PASS；JS 新增 11/11 通过、全量单测 **533/533 通过**；ESLint **0 error / 73 warning**；Ruff 通过。

## 总体评价

改动方向正确：会话级 TTL 内存缓存底座 `mem_cache` 设计干净（ns 隔离、条数与字节双上限、LRU 淘汰、参数非法静默返回），缓存键构造严谨，空结果与错误结果一律不落缓存，失效钩子覆盖面广，缓存故障全程不阻断主链路。

但存在 **2 个 P1 级语义缺陷**（并发数据静默丢失、残缺结果被永久重放）和 **1 个 P2 级算式错误**（缓存实际未生效）。**测试全绿并不代表无缺陷** —— P1 竞态未被任何用例覆盖，P2 算式错误反被现有用例的宽松断言掩盖。

---

## 一、改动概览

| 层 | 文件 | 改动 |
| --- | --- | --- |
| 新增 | `python-backend/mem_cache.py` | 进程级 TTL 内存缓存底座（143 行） |
| 后端 | `python-backend/config.py` | 站点热替换后失效 `spider:*` 四个 ns |
| 后端 | `python-backend/server.py` | spider 内容四接口 + Kazumi 搜索/章节 + SSE 流接入缓存 |
| 主进程 | `src/main/index.js` | `yuki:asset-status` 加 60s TTL 缓存 |
| 渲染层 | `src/renderer/js/timeline.js` | Bangumi 账号收藏集合加持久兜底 |
| 测试 | 3 个 Python + 1 个 JS 新套件 | 并在 `run_all.py` 注册三个新 stage |

## 二、验证结果

- Python：`test_mem_cache.py` / `test_spider_content_cache.py` / `test_kazumi_cache.py` 共 30 个用例 **ALL PASS**
- JS：`tests/js/timeline-cache.test.js` **11/11** 通过；全量 `run-jsunit.js` **533/533** 通过
- `eslint src tests scripts`：**0 error**（73 warning 均为既有 `/* global */` 声明项，非本次引入）
- `ruff check python-backend`：All checks passed

---

## 三、严重（P1）

### 1. SSE 单源索引读改写竞态 —— 并发下静默丢源

`_kazumi_stream_cache_put_source` 的「读索引 → append → 写回」三步非原子，而调用方是
`ThreadPoolExecutor(max_workers=8)` 中 8 个并发 worker 逐源完成时调用。

实测 8 线程并发 put：

```
索引最终内容: ["h"]          ← 只剩最后一个
实际写入的 payload 数: 8     ← 8 条 payload 都在
```

payload 全写入，索引只剩 1 个名字。后果：下次整词重放只吐 1 条源结果，其余 7 条源在 TTL 内
整体消失，用户看到的是「搜出来只有 1 个源」而非「搜不出来」—— 比缓存 miss 更难排查。

**修复**：把 append 收进 `mem_cache` 的锁内，新增原子 list-append 原语，不要让调用方读改写。

- `python-backend/server.py:2160` — `_kazumi_stream_cache_put_source`
- `python-backend/mem_cache.py:115` — 需新增 `append_value(ns, key, item)`

### 2. SSE 流超时产生被永久重放的部分结果

`as_completed(..., timeout=120)` 超时后走 `except` 分支直接发 `done`（`server.py:1789`）。
但超时前已完成并落缓存的源，索引已经写好。下次同词同筛选请求命中索引，重放的仍是那份
**不完整的**结果集，且无任何标记表明残缺。慢网络用户第一次超时拿到 3 个源，之后 30 分钟内
反复打开都只能看到那 3 个源。

**修复**：只在外层正常跑完时才写索引；或在 `except` 分支主动 `invalidate_prefix` 掉该
`word|fkey` 的索引与 payload，使部分结果不参与重放。

- `python-backend/server.py:1779` — `for fut in as_completed(futures, timeout=120)`

---

## 四、中等（P2）

### 3. 时间表恢复的 `_colCacheTs` 算式让内存缓存永不命中

```js
this._colCacheTs = Date.now() - Math.max(0, TIMELINE_COL_PERSIST_TTL - persisted.ageMs);
```

持久 TTL 30min、内存 TTL 5min。实测：

| 持久数据 age | 恢复后内存缓存是否命中 |
| --- | --- |
| 0 min | false |
| 1 min | false |
| 26 min | **true** |

刚落盘的持久数据恢复后反而**不**命中内存缓存，26 分钟前的旧数据却命中 —— 与「越新鲜越可信」
的直觉相反，且首开省下的网络请求会在紧接着的第二次进入时又付一次。表达式缺一个 `min`：
应取 `min(ageMs, TIMELINE_COL_TTL - 缓冲)`，让 age 直接继承为内存 age。

- `src/renderer/js/timeline.js:232`

---

## 五、次要项（P3）

- **`kazumiReorder` 漏失效钩子**（`server.py:2366`）。add / remove / toggle / 批量更新 / 商店安装
  都调了 `_kazumi_invalidate_caches`，reorder 没有。顺序影响 SSE 推送次序与前端展示序，缓存的
  单源 payload 顺序仍是旧的。影响轻，但与其余分支不一致，大概率漏写。
- **`kazumi:search` 命名空间混装两类数据**。`kazumiSearch` 全量 body 用裸 key `海贼王`，SSE 单源用
  `stream|...`。当前不冲突，但同 ns 意味着 TTL 与淘汰策略耦合，后续加前缀易撞。建议拆分为
  `kazumi:search` 与 `kazumi:stream`。
- **`_spider_body_cacheable` 把 `code: None` 当成功**（`server.py:2261`）。合法 CatVod 扁平响应确实
  无 code 字段，可接受；但意味着任何 dict 形响应只要 list 非空就缓存，守卫比 Kazumi 侧宽松。
- **`invalidate_prefix` / `stats` 无生产调用方**，仅测试使用。前者为「按站点清 spider 缓存」预留
  （当前热替换走整 ns 清）。建议注释标明是预留 API，否则易被当死代码删除。
- **`asset-status` 60s 缓存无失效钩子**。用户手动点刷新仍拿到 60s 内缓存值
  （`panels.js:2323` → 主进程直接命中缓存返回）。ffmpeg 下载中或用户装完 Java 后手动刷新，
  设置页最长延迟 60s 才反映。建议给 IPC handler 加 `force` 参数，或让手动刷新走独立通道。
- **`config.py` 失效时机在 `runner.destroy()` 之后**，注释已解释理由（防在途回填以新 key 名义
  存活），顺序正确。但 `_cached_spider_content` 的回写不受此保护：热替换瞬间若有在途请求，
  其 builder 仍会完成并回写旧内容到**新**配置名下。窗口很窄，可接受，值得补一条注释。

---

## 六、设计上肯定之处

- **缓存键构造严谨**：spider 侧按 endpoint 只纳入真正影响结果的字段且顺序固定；search 的
  `word` 正确处理了「缺省回退 `key`」别名，避免跨关键词串结果；SSE 侧把 `tag/year/sort`
  纳入筛选指纹，不同筛选互不串台。
- **空结果与错误结果一律不落缓存**：spider 的 `_spider_body_cacheable`、kazumi 的
  `_kazumi_search_body_cacheable`、chapters 的空 `roads`、SSE 的 `error` 单源 —— 避免把瞬时
  故障冻结成 TTL 内的「永久空结果」。
- **失效钩子覆盖面广**：插件 add/remove/toggle、商店安装、批量更新、Cookie 设置/清理、
  有效性检测启动、配置热替换、设置面板清缓存。
- **缓存故障一律不阻断主链路**：mem_cache 自身参数非法静默返回，server 侧再兜一层 `except`。
- **`config.py` 用逐个 ns 失效而非 `clear_all`**：避免误伤与站点配置无关的 `kazumi:*`。
- **索引失效时优雅回退**：`_kazumi_stream_cached_payloads` 在索引存在而 payload 全缺失时返回
  空列表，调用方视作 miss 回退真实检索，不发假的「无结果」。

---

## 七、建议修复顺序

1. **P1 索引竞态** —— 真实并发行为偏差，数据静默丢失
2. **P1 SSE 超时部分结果** —— 用户可感知的残缺搜索结果
3. **P2 `_colCacheTs` 算式** —— 缓存实际未生效，与改动意图相反
4. **P3 `kazumiReorder` 漏钩子** —— 一行修复，一致性问题
