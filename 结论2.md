# YuKi 项目代码审查报告

**审查范围**：Electron 主进程（14 文件 / ~11k 行）、Python 后端（~20k 行）、渲染层（17 文件 / ~16k 行）、测试与 CI。
**仓库状态**：`main` @ `d318500`，版本 0.2.4。
**自动化验证**（本次实测）：JS 单测 **500/500 通过**；ESLint **0 error / 74 warning**；Ruff 通过。

## 总体评价

工程质量高于同类项目平均水平：配置加载是严格的 prepare→validate→atomic swap 事务、能力路由是纯函数、路径穿越防护（`resolveSafe`、`cache_store` sha1 key）经逐条核查均无缺陷、凭据走 `safeStorage` 加密、`contextIsolation: true` 全量正确、构建产物与 `.venv` 未入库。

但存在 **3 个确定会持续影响用户的 bug**，其中 1 个可 100% 复现；以及 **2 处远程代码执行面的安全边界缺口**。

---

## 一、严重（确定 bug）

### 1. 解析池槽位永久泄漏 —— 第 4 次起解析永久失效

`src/main/parse-window.js:182-186`

```js
_release(slot) {
    const w = this._waiters.shift();
    if (w) w();                       // 唤醒等待者，但 slot 未归还 _slots
    else this._slots.push(slot);
}
```

等待者的 `take()`（`parse-window.js:173-178`）执行 `this._slots.shift()`，此刻池子为空 → 得 `undefined` → 把自己重新 push 回 `_waiters`。槽位就此永久丢失。

`POOL_SIZE = 3`（`parse-window.js:24`），即：**并发解析满 3 次后，后续所有 `yuki:parse`、`yuki:capture-direct`、`captchaVerify` 永久 pending**，直到重启应用。`yuki:parse` 的 25s 超时兜底只返回失败，不恢复能力。

**修复**：`if (w) w(slot);` —— 一行。

### 2. 下载去重对「已完成」恒失效 —— 必然重复下载

`src/main/dl-dedupe.js:19, 23` 定义了可注入的 `fsLike`，默认 `existsSync: () => false`；`setFs` **只在 `tests/js/dl-dedupe.test.js:20` 被调用过**，生产代码 `index.js` 从未注入真实 `fs`。

结果 `dl-dedupe.js:99` 的 `fsLike.existsSync(f)` 恒 false → `file` 恒 undefined → 放行重下。影响 `index.js:1270`（边下边播）、`:2497`（手动下载）、`:2563`（addHls）、`:3058` 四处。注释承诺的「重播不再重复下载」不成立。

**修复**：`index.js` 构造 `dlDedupe` 后调用 `setFs(fs)`。

### 3. 退出清理里 HLS 的 ffmpeg 子进程全部泄漏

`src/main/index.js:4056`：`try { if (hls && hls.cleanup) hls.cleanup(); } catch (e) {}`

`HlsDownloader` **没有 `cleanup` 方法**（全文件 grep 无命中，方法清单只有 `remove/clearStopped/pauseAll/...`）。可选链静默 no-op，`catch` 又吞掉异常。退出时有 m3u8 下载在跑 → ffmpeg（`hls-downloader.js:636` spawn）成孤儿进程继续写盘。同函数其它几项（mpv/aria2/bridge）都是真调用。

**修复**：实现 `cleanup()` 遍历 `this._tasks` 对 `t._proc` kill + `_cleanSegsDir`。

---

## 二、安全（远程数据面）

### 4. 远程 Spider/JAR 下载绕过 SSRF 与体积守卫

`python-backend/http_client.py:213-217` 的 `fetch_follow_redirects` 内部只做 `urljoin`，**全程不调用 `guard_url`**（已 grep 确认该函数在 http_client.py 中零命中）。调用方 `config.py:1670`（远程 py）、`cms_spider.py:141`、`jar_bridge.py:1263` 都只校验首跳。

对比：配置正文走 `config_security.py:486` 的 `fetch_guarded`，**每跳**过守卫、流式限长。

**影响**：远端配置可 302 → `127.0.0.1:9978`/内网，把任意大小响应拉入内存并**作为代码落盘执行**（py 走 `site_worker.py:173` `exec_module`；jar 走 JVM 反射）。私网拦截 `YUKI_CONFIG_BLOCK_PRIVATE_NETWORK` 对此路径无效。

**修复**：JAR/py/CMS 取回统一改 `fetch_guarded`，或至少在重定向循环内每跳回调 `guard_url` + `stream=True` + `read_capped`。

### 5. QuickJS 限额缺失时静默降级 + 请求参数不可信

`python-backend/js-engine/quickjs_host.py:231-238`：

```python
try:
    self.ctx.set_time_limit(30); self.ctx.set_memory_limit(256 * 1024 * 1024); ...
except AttributeError:
    logger.warning('quickjs-ng 缺少限额 API，跳过（建议升级 quickjs-ng）')
```

注释自己写明「一段 `while(true){}` 会冻结整个后端」，但 API 缺失时**仅 warning 后继续运行**。且 `quickjs_host.py:361` 的 `lock.acquire(timeout=35)` 会被挂死 JS 持有 35s。

`quickjs_host.py:191` 的 `_native_http`：`timeout` 未 clamp（JS 可传 `1e9`）、响应体非流式无上限。**SSRF 侧已正确接入 `guard_url`**（第 196-202 行，这点做得好），资源侧敞开。

**修复**：限额 API 缺失时拒绝加载 JS 站点（抛 `[L3:js]`）而非降级；`timeout` clamp 到 ≤15s 并改流式限长。

### 6. 渲染层两处 XSS + 全站无 CSP

`src/renderer/js/detail.js:385-386` 与 `home.js:1629-1631` 把后端 `data.error` / `_catError`（JAR Spider 回传文本，不可信）直接拼进 `innerHTML`，未走项目已有的 `escHtml()`。

```js
const err = data && data.error ? `（${String(data.error).slice(0, 120)}）` : '';
$('#detail-body').html(`<div class="tip-line">未取得详情${err}</div>`);
```

`src/renderer/index.html` **无 CSP meta**（已确认），主进程也未设响应头；全站约 30 处字符串模板生成 `onload=`/`onerror=`/`onclick=`。渲染层可通过 `window.yuki.*` 调 `fileDelFile`、`download.control`、`playUrl`。

已逐条核查：其余远端字段（片名/简介/评论/规则名）转义彻底，`_renderCommentBBCode` 先转义再限制 `[url]/[img]` 为 `https?:`，`escPath` 转义顺序正确。**仅此两处**是真实注入点。

---

## 三、中等（正确性 / 工程化）

**陈旧响应竞态**（渲染层）：`detail.js:371-417` 主详情链路无世代令牌（同文件的 Bangumi 补充却有用 `_bgmExtraGen`），打开 A（慢）→ 返回 → 打开 B（快）→ A 落地会整页覆盖 B。`live.js:191`、`popular.js:116`、`timeline.js:381` 同类；其中 `timeline.js:374` 的 `_saveCache()` 在写入时才读 `this._cacheKey()`，会把 A 的数据写进 **B 的缓存键**，污染 30 分钟。

**测试覆盖缺口**：5 个测试文件未接入 `run_all.py` 的 `STAGES`，其中 `test_dex2jar_lifecycle.py` **当前就是红的**（2 failures + 1 error，mock 未跟上 `creationflags` 参数变更）。孤儿测试连语法错误都不会被发现。另 `compat.py`/`jar_patch.py`/`pan_login.py`/`trigger.py` 零覆盖；JS 侧 `system-proxy.js`（改全局网络设置）零测试。

**错误掩盖**：`package.json:8` 的 `postinstall` 带 `|| exit 0`；`ci.yml:26-41` 的 `npm audit` 与 `pip-audit` 均 `|| true`。CI **不跑 ESLint**（`test:all` 里有，CI 里没有）。

**仓库卫生**：`dist/`、`python-dist/`、`.venv/`、`.omo/` 等**均未入库**（干净，已 `git ls-files` 确认）。但 `paint-order-test.png`、`vlc-help.txt`（0 字节）已入库；`scripts/` 下 **30 个 `diag-*`/`acceptance-*` 一次性调试脚本**（4834 行）入库且不在任何自动化中运行，`acceptance-t55.js:6` 自称「临时脚本」，必然腐烂。

**文档漂移**：`README.md:20` 与 `PROGRESS.md:12` 仍写 `0.2.2`，`package.json` 已 `0.2.4`；`package-lock.json` 记录的 version 也是 `0.2.2`；`ruff.toml:5` 的 `target-version = "py312"` 与 CI 的 Python 3.14 不一致。

**后端其它中等项**：

- `supervisor.py:517` 超时后无条件重建进程 → 连续慢请求会放大成「永久冷启」，超时风暴自我强化。
- `server.py:1794` `/action` 超时只 `task.cancel()`，`run_in_threadpool` 线程不会被真正中断，`_SPIDER_SEMAPHORE(16)` 在慢源下逐步泄漏，16 次后所有 `/action` 排队。
- `contracts.py:47` `RuntimeRequest` 用 `time.time()` 墙上时钟计时，时钟回拨时请求永不超时。
- `supervised_runner.py:49` 共享 `spider.last_error` 跨请求竞态，可把成功结果判成失败。
- `go_proxy.py:1876` `ThreadingHTTPServer` 未设 `daemon_threads=True`，长连接未断时挂住进程退出。
- `downloader.js:175` aria2 `error` 监听缺 `if (this.proc !== proc) return;` 迟到保护。
- `mpv-player.js:423` 整季 m3u 临时文件从不删除，累积在 `%TEMP%` 且含签名直链。

---

## 四、未发现问题的类别（明确排除）

- **Electron 安全基线**：4 处 BrowserWindow 全为 `contextIsolation: true` + `nodeIntegration: false`，隐藏解析/验证码窗口额外 `sandbox: true`；preload 仅暴露具名 IPC 方法，无 shell/fs/child_process 直通。
- **路径穿越**：`resolveSafe`（`file-manager.js:45`）、`config.py:1676` realpath 前缀校验、`cache_store` sha1 key、`config_security.py:282` 挡 `C:\` 与 `\\server` —— 均正确。
- **注入类**：无 `eval`/`new Function`（主进程 14 文件零命中）；无 `shell=True`/字符串命令（subprocess 全用 argv 列表）；无 pickle/yaml.load 反序列化。
- **密钥安全**：全仓扫描 `ghp_`/`AKIA`/PEM 私钥头零命中；两个 workflow 未引用任何 `secrets.*`。
- **配置事务与缓存**：`config.py:1078` 原子替换 + 代际守卫（防旧线程覆盖新配置）、`config_cache.py` mkstemp+fsync+os.replace+sha256 自校验、`cache_store` 原子写与配额淘汰 —— 设计正确。
- **Token 鉴权**：`server.py:86` `TOKEN_EXEMPT` 仅豁免 `/health`、`/cache`、`/proxy`，中间件精确匹配，`/sites`、`/kazumi/*`、`/runtime/cancel` 均需 token。
- **事件监听泄漏**：渲染层 `_bindSheetEvents`、`_unbindCommentPageScroll` 等均有正确 `.off()`，各视图有 `_inited` 一次性守卫。

---

## 建议修复顺序

1. `parse-window.js:184` —— `w()` 改 `w(slot)`（一行，解除全会话解析失效）
2. `index.js` 注入 `setFs(fs)` —— 恢复已完成去重
3. `index.js:4056` —— 实现 `hls.cleanup()`，堵 ffmpeg 孤儿
4. `detail.js:385` + `home.js:1629` —— 补 `escHtml`，并加 CSP
5. `http_client.py:213` —— 重定向每跳过守卫 + 流式限长
6. 把 5 个孤儿测试接入 `run_all.py`，先修 dex2jar 用例

前三项是用户可感知的确定性 bug，第 1 项并发 3 次解析后 100% 复现。
