# YuKi 审查报告核验 · 最终结论

> 核验日期：2026-09-17 · 基线：`main` @ `d318500`（与结论文档所述一致，工作区除两份报告外无改动）
> 核验对象：`结论.md`（8 P1 + 27 P2 + 正面清单）与 `结论2.md`（3 严重 bug + 3 安全缺口 + 中等项 + 排除清单）
> 核验方法：约 70 条断言逐条对照代码取证（4 路独立核查），可运行项实测复现（JS 单测 / ESLint / Ruff / dex2jar 用例 / Node 行为复现），关键竞态（解析池）由主控直接推演验证。

---

## 一、总体判定

**两份文档的综合结论准确、可作为整改依据。** 逐条核验结果：

| 判定 | 数量 | 说明 |
|---|---|---|
| CONFIRMED（属实） | 56 | 全部 P1/严重级问题均在列 |
| PARTIAL（大体属实，细节有偏差） | 10 | 问题成立但行号、数量、机制或后果描述需修正，见勘误表 |
| REFUTED（不属实） | 1 | mpv 整季 m3u「从不删除、累积」——仓库现存在双重清扫路径 |

- **结论.md 的 8 个 P1 全部属实**，其「所有 P1 发现均已人工复核」的自述成立。
- **结论2.md 的 6 个严重/安全项全部属实**，且其自报的实测数字与本次复跑**完全吻合**：JS 单测 500/500 通过、ESLint 0 error / 74 warning、Ruff 通过、`test_dex2jar_lifecycle.py` 当前确为红（5 用例 2 failures + 1 error，原因正是 mock 未跟上 `creationflags` 参数变更）、5 个孤儿测试文件清单逐一命中。
- 两文档重叠的断言（去重失效、HLS 清理缺失、两处 XSS、版本漂移、仓库卫生、CI 缺 ESLint、无 CSP）互相印证，全部成立。
- 两文档独有的高价值发现互补而非重复：结论.md 独有 go_proxy 异常类型错捕、`/proxy` 开放代理、python-bridge 缓冲泄漏、destroy_all 死锁；结论2.md 独有解析池槽位泄漏、`fetch_follow_redirects` 绕守卫、QuickJS 静默降级。**合并后的必修清单共 11 项。**

## 二、合并确认清单（P1 / 严重，11 项）

| # | 问题 | 位置 | 来源 | 核验 |
|---|---|---|---|---|
| 1 | 解析池槽位永久泄漏：`_release` 唤醒 waiter 时不归还槽位，waiter 的 `take()` 从空池 shift 得 `undefined` 后自我重排 → 每次「有等待者的释放」永久丢 1 槽；POOL_SIZE=3，一波 4 并发后 `yuki:parse`/`capture-direct`/`captchaVerify` 全部永久 pending，须重启 | `parse-window.js:171-186` | 结论2 | ✅ 逐行推演属实，100% 可复现 |
| 2 | 下载去重「已完成」恒失效：`DEFAULT_FS.existsSync` 恒 false，生产从未 `setFs()`（全仓唯一调用点是测试）→ 四处调用点恒放行重下 | `dl-dedupe.js:19`；影响 `index.js:1270/2497/2563/3058` | 两文档 | ✅ |
| 3 | 退出清理调用不存在的 `hls.cleanup()`，HLS 任务 spawn 的 ffmpeg（`hls-downloader.js:522/553/636/725`）成孤儿 | `index.js:4056` | 两文档 | ✅ |
| 4 | 后端 stdout 缓冲无限增长：READY 匹配后 `buf` 不截断不重置，内存与正则 CPU 随日志线性涨 | `python-bridge.js:102-119` | 结论1 | ✅ |
| 5 | 崩溃重启定时器与 stop/start 竞态 → 双后端：`setTimeout` 句柄未存，`_spawn` 无「已有 proc」防重入（仅 `stopping` 标志），**`stop();start()` 连调窗口内旧定时器仍会触发**；该连调序列在 `index.js:1842/1991/2000/2183` 真实存在 | `python-bridge.js:136/190` | 结论1 | ✅（触发条件比原文更收窄：单纯崩溃后 stop 会被 stopping 兜住） |
| 6 | `JarBridge.destroy_all()` 死锁：持不可重入 `threading.Lock` 循环调 `destroy()`，后者再次 acquire 同一锁 → 永久挂死；除退出路径（`server.py:1537`）外，**配置热重载路径（`site_manager.py:187`）同样触发**；同文件 `_evict_jvm_if_needed_locked` 已示范正确模式 | `jar_bridge.py:263/409-412/1221` | 结论1 | ✅ |
| 7 | `go_proxy._put` 错捕 `queue.Empty`（实抛 `queue.Full`）：首个 Full 被宽 except 吞掉并**整段重下 3 次（向已满队列重复灌 chunk，破坏字节序）**，第 3 次失败后收尾 `_put` 裸奔炸死下载线程、无哨兵；`stream()` 的 `q.get()` 无超时无取消检查 → 播放流永久挂死。触发：≥32MB Range 分段（`go_proxy.py:1785-1788`）+ 背压 | `go_proxy.py:1158-1160/1198/1212` | 结论1 | ✅（后果比原文描述更重） |
| 8 | `/proxy` 数据面免 token + `?url=` 任意 http(s) 转发、无 guard_url/私网限制（全文件 0 命中）：本机任意非浏览器进程可当开放代理打内网；`_reject_browser` 拦不住非浏览器。jar 侧播放 URL 已带 token（`jar_bridge.py:557-566`、`Proxy.java:61-63`），**加鉴权无兼容障碍** | `server.py:86` `hoststate.py:105` `go_proxy.py:1340-1343` | 结论1 | ✅ |
| 9 | **远程 py/jar/CMS 下载绕过 SSRF 与体积守卫 → 代码执行链**：`fetch_follow_redirects` 重定向循环只做 `urljoin`、全文件 `guard_url` 零命中、非流式全量入内存；调用方（`config.py:1670`、`cms_spider.py:141`、`jar_bridge.py:1263`）只守首跳。远端配置 302 → 内网/超大响应可落盘并经 `site_worker.py:178 exec_module` 执行，`YUKI_CONFIG_BLOCK_PRIVATE_NETWORK` 对此路径无效 | `http_client.py:213-217` | 结论2 | ✅ |
| 10 | QuickJS 限额 API 缺失时静默降级（仅 warning 继续跑，注释自认 `while(true){}` 冻结后端）；`_native_http` timeout 未 clamp、响应体非流式无上限（SSRF 侧 `guard_url` 接入正确） | `quickjs_host.py:231-240/191-216/361` | 结论2 | ✅ |
| 11 | 渲染层 XSS：后端透传 `data.error`/`_catError`（第三方源可控）截断不转义直入 `.html()`，全站无 CSP（meta 与响应头均无）。**核验新发现第三处漏网**：`common.js:455 bangumiCard` 的 `rating.rank`（Bangumi 远端字段）未转义进属性位，且 `timeline.js` 全程不调用 escHtml（ESLint 亦报 unused） | `home.js:1629-1630` `detail.js:385-386` `common.js:432-447/455` | 两文档+核验 | ✅（两文档「仅此两处」均低估） |

**结论.md「无 P0」口径说明**：其定义限定为「渲染进程可任意读写/执行」，该口径下判断成立（D1-D14 正面清单核验 13/14 属实）。但第 9 项（远端配置驱动的代码执行链）实质构成远程数据面 RCE 面，两文档分歧处**以结论2.md 为准**：结论.md 把「配置层 SSRF」列入"确认无问题"仅对配置正文路径（`fetch_guarded`）成立，下载面绕行被漏判。同理，结论2.md 把 TOKEN_EXEMPT 含 `/proxy` 列入"无问题"只核对了机制与清单一致，风险判断以结论.md 第 8 项为准。

## 三、勘误表（PARTIAL 10 条 + REFUTED 1 条 + 修复方案错误 1 条）

| 断言 | 来源 | 修正 |
|---|---|---|
| 「`ThreadingHTTPServer` 未设 daemon_threads → 非 daemon 线程阻塞退出」 | 结论2·中等 | **机制错误**：CPython `ThreadingHTTPServer` 类定义自带 `daemon_threads=True`（3.14 实测）。但「退出挂住」结论仍成立：`block_on_close=True` 使 `server_close()` join 全部 handler 线程，配合第 7 项的无超时 `q.get()` 挂死线程即卡退出 |
| 「supervisor 超时重建 → 永久冷启自我放大」 | 结论2·中等 | 重建属实，放大不成立：熔断器（连续 3 次同阶段失败 open 60s）封顶，最多 3 连杀后拒绝重建 |
| 「聚合搜索每请求 16 线程池；kazumi 超时 shutdown 泄漏非 daemon worker」 | 结论1·P2-12 | 池为惰性创建、上限按站点数；kazumi 池上限是 8 非 16；滞留线程会随请求结束退出，属重复建池开销+滞留，非永久泄漏 |
| 「主窗缺 will-navigate（parse-window/pan-qr 都有）」 | 结论1·P2-1 | 半真：主窗**有** `setWindowOpenHandler`（`index.js:571-576`），缺的是 `will-navigate`（全仓仅 parse-window 有两处）；pan-qr-window 同样只有 openHandler 无 will-navigate |
| 「playlist-proxy Range 越界靠外层 catch 回 502」 | 结论1·P2-18 | 实测**更糟**：`writeHead(-N)` 不抛错且 headersSent，随后 `createReadStream` 同步抛错，外层 502 因 headers 已发送再抛并被吞 → 客户端收不到任何响应（挂死），非回 502 |
| 「push-server body >64KB 悬挂不响应」 | 结论1·P2-7 | `req.destroy()` 立即断连（ECONNRESET），非无限悬挂；其余三点（0.0.0.0、畸形请求行抛进全局、token 走 query）实测属实 |
| 「二进制供应链：全部 lock+sha256，已存在文件也复验」 | 结论1·正面清单 | 「已存在复验」仅 anime4k/misans 成立；mpv/aria2 存量 exe 只 `existsSync` 短路（`download-binaries.js:169/238`），lock 锁的是压缩包哈希，无 exe 期望值 |
| 「live.js 等无世代令牌」 | 结论2·中等 | live.js 有 `_probeToken`，但 await 后到渲染前未校验——竞态成立，措辞不准；popular/timeline/detail 主链竞态（含 `timeline.js:374` 缓存键污染）全部核实成立 |
| 「diag/acceptance 一次性脚本 30 个 / 4834 行」 | 结论2·卫生 | 实测 27 个 / 4066 行；入库垃圾（png、0 字节 vlc-help.txt、pnpm-lock 并存）属实 |
| 「pan_login.py:186 死条件」 | 结论1·P2-11 | 行号应为 :188-189；死条件判断属实（遍历 cookie 名恒不含域名），且兜底分支无任何过滤 |
| 「mpv 整季 m3u 临时文件从不删除、累积 %TEMP%」 | 结论2·中等 | **不成立（REFUTED）**：`mpv-player.js:415-421` 每次写新清单前 rmSync 清扫全部历史清单，`index.js:883-891` 启动时二次清扫。仅残留「最后一次播放的清单活到下次启动」，「累积」错误 |
| 「修复：`if (w) w(slot);` 一行」 | 结论2·#1 | **该行级修复无效**：`take` 是零参闭包，传入的 slot 被忽略，仍从空池 shift。正确一行改法是 `_release` 里 **先 `this._slots.push(slot)` 再唤醒 w()**；或让 take 接收 assigned 参数 |

另有小误不影响判定：`valid_proxy_token`/compare_digest 在 `hoststate.py:105` 而非「server.py 同文件」；`system-proxy.js` 的 execSync 仅在未配置手动/环境变量代理时触发；`dl add` 的 `..` 因分隔符已被清洗不构成穿越，危害限于畸形文件名；`captcha-verify` 超时、`kazumi.js:1185` 无限轮询、`downloader.js:175` 缺迟到守卫、`contracts.py:47` 墙上时钟、`server.py:1794` 信号量滞留、`push-url` 无协议白名单、ffmpeg 无 sha256 pin 且 `https.get` 无 timeout、`kazumiImageSearch` 无限长、`main token !=` 非恒定时间、`server.py:2256`/`proxy_gateway.py:26` 死代码、Anime4K ghfast.top 镜像+子串校验、版本 0.2.4/0.2.2 四处漂移、ruff py312 vs CI 3.14、README/PROGRESS 落后、CI 不跑 ESLint、postinstall `|| exit 0`、audit `|| true`、release 不等 CI——**以上全部 CONFIRMED**。

## 四、修正后的建议整改顺序

1. **解析池泄漏**（#1）——用户可感知、100% 复现；用「先 push 槽位再唤醒」的正确一行式修复，勿照抄结论2.md 的 `w(slot)`。
2. **XSS 三处补 escHtml + 渲染层 CSP + 主窗 will-navigate**（#11 + P2 安全类）。
3. **`fetch_follow_redirects` 统一改走 fetch_guarded**（#9，远程代码执行面，安全优先级最高的存量缺口）。
4. **`/proxy` 的 `?url=` 通道要求 token + 私网拦截**（#8，jar 侧已注入 token，无兼容障碍）。
5. **去重注入真实 fs**（#2，一行）+ **`hls.cleanup()` 实现**（#3）。
6. **destroy_all 锁外销毁**（#6）+ **go_proxy 改捕 `queue.Full`、`q.get` 加超时与取消检查**（#7）。
7. **python-bridge 截断 buf、保存并取消重启 timer、`_spawn` 加 proc 防重入**（#4/#5）。
8. **QuickJS 限额缺失时 fail-closed 拒载 JS 站点 + timeout clamp + 流式限长**（#10）。
9. P2 按需：ffmpeg sha256 pin + 下载超时、push-url 协议白名单、mpv/aria2 存量文件复验、陈旧响应世代令牌、`/action` 信号量超时获取、dex2jar 红用例与 5 个孤儿测试接入 STAGES、CI 补 lint、版本/文档对齐、仓库垃圾清理。

## 五、对两份文档本身的可信度结论

- **结论.md**：P1 判定 8/8 属实、正面清单 13/14 属实，是可靠的；失准点集中在少数 P2 的后果描述（A9 的 502、D7 的复验范围、A14 的半真）与「配置层 SSRF 无问题」的覆盖面误判（下载面绕行）。
- **结论2.md**：独有发现质量最高（解析池泄漏、fetch_follow_redirects 绕守卫是两份报告中仅有的两个「必然触发/可被执行链利用」级缺陷），自报实测数据 100% 可复现；失准点在 daemon_threads 归因错误、mpv 临时文件一条不成立、以及第 1 项的修复代码字面上无效。
- 综合：**合并 11 项必修 + 上表 12 条勘误后执行**，即得到经完整证据链支撑的整改基线。
